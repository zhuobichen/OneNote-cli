# OneNote CLI

> 命令行直接读写本地 OneNote 笔记本。

## 说明

Windows only，依赖 Microsoft Office OneNote Desktop 的 Interop 接口（`Microsoft.Office.Interop.OneNote`）。

## 使用

```powershell
python onenote_cli.py --help
# 或通过 onenote.bat 调用
```

输出 JSON，便于脚本与 AI 工具集成。
